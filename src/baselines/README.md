# Baselines

Future home of random-weight, simple trained, and advanced frozen baseline adapters.

All baselines must reuse Stage 0 window manifests, transforms, split identity,
and later metric code. Stage 0 runs no baselines; the course meaning of a
random-initialized CUT3R baseline remains open.
