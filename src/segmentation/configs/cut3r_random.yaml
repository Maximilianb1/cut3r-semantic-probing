experiment: segmentation-cut3r-random
backbone: cut3r-random

# Random-initialized CUT3R baseline. Its scientific meaning is an OPEN decision
# (see ../../backbones/README.md and PROJECT_STATUS); the cache it
# reads comes from a provisional reset_parameters init pending a baseline ADR.
# Smoke-test against the fake caches without editing this file:
#   CUT3R_CACHE_ROOT=src/segmentation/dummy_embeddings python -m src.segmentation.train_segmentation --config <this file>
probe_cache:
  dir: ${CUT3R_CACHE_ROOT}/probe/cut3r-random

model:
  feature_dim: 768
  num_classes: 1
  # [] is a true linear probe; [512] is a 1-hidden-layer MLP probe.
  # Which one Stage 1 reports is an OPEN decision.
  hidden_dims: [512]
  activation: gelu
  dropout: 0.0

training:
  # adam | adamw | sgd (sgd also reads `momentum`)
  optimizer: adam
  epochs: 20
  batch_size: 16
  lr: 0.001
  weight_decay: 0.0
  pos_weight: null
  seed: 20260729
  device: cpu
  num_workers: 0
  # tqdm bars; they draw only on a real terminal, never into a redirected log
  progress: true

splits:
  train: train
  val: val
  test: test

output:
  dir: src/segmentation/experiments/segmentation-cut3r-random
